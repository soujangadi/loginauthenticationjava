package com.rakuten.ems.DateTime; /*  int size(),
// *boolean is empty() ,boolean contains(object obj),boolean add(element e)
*boolean remove(objective obj),boolean containsall(collection c),boolean addall(collection c),
*boolean removeall(collection c),boolean retain all( collection c),boolean rea
*/

public class coll {

}
